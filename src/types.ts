export interface User {
  id: string;
  username: string;
  email: string;
  color: string;
  profilePicture: string;
  initials: string;
  weekStartDay: number;
  globalFontSupport: boolean;
  timezone: string;
}
